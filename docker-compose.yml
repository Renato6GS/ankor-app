services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ankor
      POSTGRES_PASSWORD: ankor123
      POSTGRES_DB: ankor
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
